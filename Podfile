# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'custom_iMessage_clone' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for custom_iMessage_clone
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'SwiftyRSA'  
  pod 'KeychainSwift', '~> 13.0'
  pod 'RNCryptor', '~> 5.0'
  target 'custom_iMessage_cloneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'custom_iMessage_cloneUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
